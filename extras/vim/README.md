# Vim ColorScheme

Clone the repo somewhere.

```sh
git clone https://github.com/jadonwb/arrowlake.nvim ~/projects/arrowlake.nvim
```

Add the below to your `~/.vimrc` file.

```vim
set termguicolors
set rtp+=~/projects/arrowlake.nvim/extras/vim
colorscheme arrowlake
```
