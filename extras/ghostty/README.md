Create ` ~/.config/ghostty/themes`
and download theme file into `themes` directory.
Then in `~/.config/ghostty/config` chose the theme.

Example:

```ini
theme = "arrowlake_dark"
```

For full list of installed themes:
`ghostty +list-themes`
